/**
 * Test stub for the `server-only` package.
 *
 * The real module throws on import outside a React Server Component, which
 * would make server modules untestable. Aliased in vitest.integration.mts only
 * -- the real guard still protects the application bundle.
 */
export {};
